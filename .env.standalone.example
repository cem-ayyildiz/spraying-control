# Copy to .env and fill in, then:
#   docker compose -f docker-compose.standalone.yml up -d --build

# Your Home Assistant URL, reachable from wherever this container runs.
HA_URL=https://home.example.com

# Profile (bottom-left, your name) -> Security -> Long-Lived Access Tokens -> Create Token
HA_TOKEN=
